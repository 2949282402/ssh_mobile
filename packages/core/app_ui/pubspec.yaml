name: app_ui
description: Shared application theme, responsive metrics, and cross-feature UI widgets.
publish_to: none
resolution: workspace

environment:
  sdk: '>=3.12.0 <4.0.0'
  flutter: '>=3.44.0'

dependencies:
  flutter:
    sdk: flutter
  animations: ^2.2.0
  shadcn_ui: ^0.56.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
