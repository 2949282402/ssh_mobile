/// Module 的稳定生命周期状态。
///
/// 初始化过程中的短暂中间态不暴露为公共状态，具体实现应在操作完成后
/// 更新状态；这样调用方只依赖可观察且可复用的稳定阶段。
enum ModuleState {
  /// 已注册但尚未初始化。
  registered,

  /// 已完成资源和依赖初始化，但当前未激活。
  initialized,

  /// 已开始运行时活动。
  active,

  /// 已停用但仍可以重新激活。
  inactive,

  /// 已释放，不允许再次使用。
  disposed,
}
