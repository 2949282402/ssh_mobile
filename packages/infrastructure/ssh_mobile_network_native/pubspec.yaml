name: ssh_mobile_network_native
description: "Native FFI SDK package for SSH Mobile P2P network platform."
version: 0.1.0

environment:
  sdk: ^3.12.2

resolution: workspace

dependencies:
  code_assets: ^1.0.0
  ffi: ^2.1.4
  hooks: ^2.1.0

dev_dependencies:
  # 使用宿主 Flutter 应用的测试运行时，避免与工作区 analyzer 版本产生分叉。
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
