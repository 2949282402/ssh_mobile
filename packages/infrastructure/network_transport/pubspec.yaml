name: network_transport
description: Stable app-scope network transport facade for SSH Mobile.
publish_to: none
resolution: workspace

environment:
  sdk: '>=3.12.0 <4.0.0'

dependencies:
  app_core:
    path: ../../core/app_core
  ssh_mobile_network_native:
    path: ../ssh_mobile_network_native

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter
