name: network_sdk
description: Typed Flutter client facades for the SSH Mobile network runtime.
publish_to: none
resolution: workspace

environment:
  sdk: '>=3.12.0 <4.0.0'

dev_dependencies:
  flutter_lints: ^6.0.0
  test: 1.31.1
  flutter_test:
    sdk: flutter
