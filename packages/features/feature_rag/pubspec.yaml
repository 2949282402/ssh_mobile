name: feature_rag
description: RAG knowledge-base feature with metadata storage and bounded local cache.
publish_to: none
resolution: workspace

environment:
  sdk: '>=3.12.0 <4.0.0'
  flutter: '>=3.44.0'

dependencies:
  flutter:
    sdk: flutter
  app_core:
    path: ../../core/app_core
  archive: ^4.0.9
  drift: ^2.34.3
  drift_flutter: ^0.3.1
  file_picker: ^12.0.0-beta.7
  http: ^1.6.0
  intl: ^0.20.3
  path: ^1.9.1
  path_provider: ^2.1.6
  provider: ^6.1.5+1
  uuid: ^4.6.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.15.1
  drift_dev: ^2.34.5
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
