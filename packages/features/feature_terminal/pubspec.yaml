name: feature_terminal
description: Terminal feature, terminal history storage, and route-scoped terminal UI.
publish_to: none
resolution: workspace

environment:
  sdk: '>=3.12.0 <4.0.0'
  flutter: '>=3.44.0'

dependencies:
  app_core:
    path: ../../core/app_core
  app_ui:
    path: ../../core/app_ui
  drift: ^2.34.3
  drift_flutter: ^0.3.1
  flutter:
    sdk: flutter
  path: ^1.9.1
  path_provider: ^2.1.6
  provider: ^6.1.5+1
  ssh_core:
    path: ../../infrastructure/ssh_core
  xterm:
    path: ../../../third_party/xterm
  animations: ^2.2.0
  shadcn_ui: ^0.56.1

dev_dependencies:
  build_runner: ^2.15.1
  drift_dev: ^2.34.5
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter
