name: feature_mcp
description: Local MCP server, approval policy, and activity console feature.
publish_to: 'none'

environment:
  sdk: '>=3.12.0 <4.0.0'
  flutter: '>=3.44.0'

resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  app_core:
    path: ../../core/app_core
  app_ui:
    path: ../../core/app_ui
  drift: ^2.34.3
  drift_flutter: ^0.3.1
  provider: ^6.1.5+1

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.15.1
  drift_dev: ^2.34.5
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
