# Run with `dart run ffigen --config ffigen.yaml`.
name: SshMobileQuicNativeBindings
description: |
  Bindings for `src/ssh_mobile_quic_native.h`.

  Regenerate bindings with `dart run ffigen --config ffigen.yaml`.
output: 'lib/ssh_mobile_quic_native_bindings_generated.dart'
headers:
  entry-points:
    - 'src/ssh_mobile_quic_native.h'
  include-directives:
    - 'src/ssh_mobile_quic_native.h'
ffi-native:
comments:
  style: any
  length: full
