[package]
name = "network-relay-proto"
version = "0.1.0"
edition = "2021"
description = "Relay control/data plane v2 wire codec (prost codegen over protocol/proto/relay/v2/relay_v2.proto)"
license = "MIT"

[dependencies]
prost = "0.13"

[build-dependencies]
prost-build = "0.13"
protoc-bin-vendored = "3"

[dev-dependencies]
serde_json = "1"
walkdir = "2"
