[package]
name = "network-core"
version = "0.1.0"
edition = "2021"

[dependencies]
network-protocol = { path = "../network-protocol" }
tokio = { version = "1.38", features = ["full"] }
tracing = "0.1"
thiserror = "1.0"
