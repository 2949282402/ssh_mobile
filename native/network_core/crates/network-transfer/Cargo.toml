[package]
name = "network-transfer"
version = "0.1.0"
edition = "2021"

[dependencies]
network-core = { path = "../network-core" }
network-protocol = { path = "../network-protocol" }
tokio = { version = "1.38", features = ["fs", "io-util", "sync"] }
sha2 = "0.10"
hex = "0.4"
tracing = "0.1"
thiserror = "1.0"
